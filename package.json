{
  "name": "telegram-bot-mirror",
  "version": "1.0.0",
  "description": "Telegram Bot deployed on Vercel via GitHub",
  "main": "api/bot.js",
  "scripts": {
    "start": "node api/bot.js",
    "deploy": "echo 'Deploy via GitHub -> Vercel'"
  },
  "dependencies": {
    "telegraf": "^4.16.3",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
